# CockroachDB / Postgres connection URL
DATABASE_URL=

# Comma-separated Kafka broker addresses
KAFKA_BROKERS=localhost:9092

# friends-service endpoint for top friend IDs (fanout); userId is appended as query param
FRIEND_IDS_FETCH_URL=http://localhost:4042/api/v1/friends/top-friend-ids
